import React from 'react';

import styled from 'styled-components';

const HomeTitle = styled.div`
  font-size: 100px;
  font-weight: 700;
  text-align: center;
  height: 600px;
  line-height: 600px;
`
export default () => {
  return <>
    <HomeTitle>
      SAFETY LEGISLATION
    </HomeTitle>
  </>;
};
